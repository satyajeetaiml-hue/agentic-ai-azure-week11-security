APP_ENV=local
LOG_LEVEL=INFO

# ── Microsoft Entra ID ───────────────────────────────────────────────────
# Set BOTH to enforce real bearer-token validation. Unset = dev mode
# (identity comes from X-Debug-User / X-Debug-Roles headers — never in prod).
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
